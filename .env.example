# Optional SQLAlchemy database URL.
# Leave unset to use artifacts/retail_segmentation.db.
# The application does not automatically load this file; export the variable
# through the shell or deployment platform unless environment loading is added.

# DATABASE_URL=postgresql+psycopg://user:password@host:5432/meridianai
